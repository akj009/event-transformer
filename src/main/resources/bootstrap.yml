server:
  port: 8082
spring:
  application:
    name: event-tranformer
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/event_transformer
    username: root
    password: root@123